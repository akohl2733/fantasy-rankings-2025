services:

  frontend:
    build:
      context: ./frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend

  db:
    image: postgres:15
    container_name: fantasy-db
    restart: always
    environment:
      POSTGRES_USER: fantasy_user
      POSTGRES_PASSWORD: secret
      POSTGRES_DB: players
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  backend:
    build:
      context: ./backend
    container_name: fantasy-backend
    restart: always
    depends_on:
      - db
    environment:
      DATABASE_URL: postgresql+psycopg2://fantasy_user:secret@db:5432/players
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/core

  nginx-proxy:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    extra_hosts:
      - "fantasy-api.internal:host-gateway"
      - "fantasy-frontend-app.internal:host-gateway"
    depends_on:
      - backend
      - frontend

volumes:
  postgres_data: